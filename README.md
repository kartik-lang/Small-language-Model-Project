Built and trained a transformer-based language model on the TinyStories dataset using PyTorch, achieving stable loss convergence and generating coherent text with ~85–90% grammatical accuracy during evaluation
Optimized training performance using mixed precision, gradient accumulation, and cosine learning rate scheduling, reducing training time by ~30% while maintaining model performance
Analyzed model outputs and loss trends to evaluate performance improvements, enabling iterative tuning that improved text coherence and reduced perplexity over training cycles
